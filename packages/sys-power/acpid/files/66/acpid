[main]
@type = longrun
@name = acpid
@description = "Run the acpid daemon"
@user = ( root )
@options = ( log env )

[start]
@build = auto
@execute = (
    execl-cmdline -s { acpid ${CMD_ARGS} }
)

[environment]
!CMD_ARGS=--foreground

