Source: Gentoo (Aaron Griffis)
Upstream: No
Reason:

	This patch fixes bug 66712 by installing the perl bindings in vendor
	instead of site.  The INSTALLDIRS bit is stolen from perl-module.eclass

	- 11 Nov 2004 agriffis
	- 07 Sep 2018 updated for subversion-1.10.2

diff --git a/Makefile.in.orig b/Makefile.in
index f4c6e61..82a6b67 100644
--- a/Makefile.in.orig
+++ b/Makefile.in
@@ -864,7 +864,7 @@ $(SWIG_PL_DIR)/native/Makefile.PL: $(SWIG_SRC_DIR)/perl/native/Makefile.PL.in
 	./config.status subversion/bindings/swig/perl/native/Makefile.PL
 
 $(SWIG_PL_DIR)/native/Makefile: libsvn_swig_perl $(SWIG_PL_DIR)/native/Makefile.PL
-	cd $(SWIG_PL_DIR)/native; $(PERL) Makefile.PL PREFIX=$(prefix)
+	cd $(SWIG_PL_DIR)/native; $(PERL) Makefile.PL PREFIX=$(prefix) INSTALLDIRS=vendor
 
 # There is a "readlink -f" command on some systems for the same purpose,
 # but it's not as portable (e.g. Mac OS X doesn't have it).  These should
