# Copyright 2018 Tom Briden <tom@decompile.me.uk>
# Distributed under the terms of the GNU General Public License v2

require github [ user=JuliaStrings tag=v${PV} ]

SUMMARY="a small, clean C library that provides Unicode normalization,
case-folding, and other operations for data in the UTF-8 encoding."

LICENCES="MIT"
SLOT="0"
PLATFORMS="~amd64 ~armv7 ~armv8 ~x86"
MYOPTIONS=""

DEPENDENCIES="
"

DEFAULT_SRC_INSTALL_PARAMS=( prefix=/usr/$(exhost --target) )

#tests tries to download a bunch of files that gets blocked by sydbox
RESTRICT="test"

