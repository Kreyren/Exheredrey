[main]
@type = longrun
@name = dnsmasq
@description = "The dnsmasq resolver"
@user = ( root )
@options = ( log env )

[start]
@build = auto
@execute = (
    execl-cmdline -s { dnsmasq -d ${CMD_ARGS} }
)

[environment]
!CMD_ARGS=--
